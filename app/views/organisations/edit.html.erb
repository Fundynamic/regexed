<div class="col-md-4 col-md-offset-4">
  <h1>Profiel</h1>
  <p>
    Vul hier hoe je organisatie heet. Wat je vooral doet en in welke skills je <em>altijd</em> interesse hebt.
  </p>
  <%= form_for @organisation do |f| %>
    <%= f.error_messages %>

    <div class="form-group">
      <%= f.label :name %><br />
      <%= f.text_field :name, autofocus: true, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :pitch %><br />
      <%= f.text_area :pitch, cols: 75, rows: 4, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :skills %><br />
      <%= f.text_field :skills, class: "form-control" %>
    </div>

    <div class="actions">
      <%= f.submit "Opslaan", class: "btn btn-primary form-control" %>
    </div>
  <% end %>

</div>
